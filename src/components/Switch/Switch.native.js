// We will still use the abstract items here. only the imports and ux could be
// different for react native

import React from 'react';


export const Switch = () => {
    return (
      null
    );
};
